function findMultSum (a, b, ceiling) {
    var sum = 0;
    for (num = 1; num < ceiling; num++) {
	if ((position % a) === 0 || (position % b) === 0) {
	    sum = sum + number;
	};
    }
    return sum;
}

